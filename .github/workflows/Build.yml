name: Build OrangeFox Recovery - tanzanite

on:
  workflow_dispatch:
    inputs:
      MANIFEST_URL:
        description: 'OrangeFox Manifest URL'
        required: true
        default: 'https://gitlab.com/OrangeFox/sync.git'
      MANIFEST_BRANCH:
        description: 'OrangeFox Manifest Branch'
        required: true
        default: '12.1'
      DEVICE_TREE_URL:
        description: 'Device Tree Repo URL'
        required: true
        default: 'https://github.com/Syntax/device_xiaomi_tanzanite_recovery'
      DEVICE_TREE_BRANCH:
        description: 'Device Tree Branch'
        required: true
        default: 'fox_12.1'
      DEVICE_PATH:
        description: 'Device Path'
        required: true
        default: 'device/xiaomi/tanzanite'
      DEVICE_NAME:
        description: 'Device Codename'
        required: true
        default: 'tanzanite'
      LUNCH_TARGET:
        description: 'Lunch Combo'
        required: true
        default: 'twp_tanzanite-eng'

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
    - name: Free Disk Space
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: true
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: true

    - name: Setup Swap
      run: |
        sudo fallocate -l 12G /swapfile
        sudo chmod 600 /swapfile
        sudo mkswap /swapfile
        sudo swapon /swapfile

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          gperf build-essential libssl-dev libxml2-utils xsltproc \
          unzip fontconfig python3 python-is-python3 repo git \
          bc ccache zip curl libstdc++6 openjdk-11-jdk

    - name: Git Config
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"

    - name: Sync OrangeFox Source
      run: |
        mkdir -p ~/OrangeFox_sync
        cd ~/OrangeFox_sync
        git clone https://gitlab.com/OrangeFox/sync.git sync
        cd sync
        ./orangefox_sync.sh --branch 12.1 --path ~/orangefox

    - name: Clone Tanzanite Recovery Tree
      run: |
        cd ~/orangefox
        git clone https://github.com/DVWorkspaces/device_xiaomi_tanzanite_recovery -b fox_12.1 ${{ github.event.inputs.DEVICE_PATH }}

    - name: Run Build
      run: |
        cd ~/orangefox
        
        # Remove vendorsetup.sh if it forces incompatible lunch combos
        rm -f device/xiaomi/tanzanite/vendorsetup.sh
        
        source build/envsetup.sh
        
        # Export required OrangeFox & compilation flags
        export ALLOW_MISSING_DEPENDENCIES=true
        export FOX_BUILD_TYPE="Unofficial"
        export LC_ALL="C"
        
        # Try lunch targets matching tanzanite setup
        lunch twp_tanzanite-eng || lunch fox_tanzanite-ap2a-userdebug || lunch omni_tanzanite-userdebug
        
        mka recoveryimage -j$(nproc --all)

    - name: Upload Recovery Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: OrangeFox-tanzanite-$(date +%Y%m%d)
        path: |
          ~/orangefox/out/target/product/tanzanite/OrangeFox-*.zip
          ~/orangefox/out/target/product/tanzanite/recovery.img
          ~/orangefox/out/target/product/tanzanite/boot.img
          
